---
label: Video 2
hide_body: true
fields:
- name: block
  type: text
  config:
    required: false
  label: Block
  default: video-2
- name: background
  type: include
  config: {}
  template: partial-background
  label: Background
- name: video_file
  type: file
  config:
    maxSize: 64
  label: Video file
  description: Upload șelf hosted video.
- name: video_image
  type: file
  config:
    maxSize: 64
  label: Video image
